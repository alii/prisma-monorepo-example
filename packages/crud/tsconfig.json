{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"rootDir": "./src",
		"outDir": "./dist",
		"tsBuildInfoFile": "../../.build-cache/crud.tsbuildinfo"
	},
	"include": ["src"],
	// We must add the packages that crud uses so TypeScript knows
	// where to fetch the type declarations from and how to build it
	"references": [
		{
			"path": "../example"
		}
	]
}
